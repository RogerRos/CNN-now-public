copia de seguretat de IA (cotxe o moto).

Distingeix entre imatges de cotxes i de motos, cosa que tambe es pot aplicar a cualsevol altra imatge en general si 
disposes de mes de 64.000 fotos.

Aquesta IA ha sigut entrenada amb mes de 64.000 imatges de cotxes i de motos, te una fiabilitat de aprox(98.5%).

El creador es diu Roger Ros el mes guapo e intel·ligent pero curiosament sense novia perque socialitzar se li dona millor a un
caragol frances.

El model va ser entrenat durant 1.5 hores seguides, va requerir una dedicacio de 2 dies per completarlo i 
no tindre temps de fer els deures de matemnatiques.

Finalment recordeu que Roger Ros es el millor i que si dieu 3 vegades el seu nom li deureu 1€.



































poso ula altre vegada el codi per si de cas:

from tensorflow.keras.models import Sequential
from tensorflow.keras.layers import Conv2D, MaxPooling2D, Flatten, Dense
from tensorflow.keras.preprocessing.image import ImageDataGenerator

# arquitectura de la red neuronal convulcional per distingir entre gossos y gats.

def IA_create():
    model = Sequential()
    
    model.add(Conv2D(64,(5,5), input_shape=(64,64,3), activation="relu"))
    model.add(MaxPooling2D(pool_size=(4,4)))

    model.add(Conv2D(128,(4,4), activation="relu"))
    model.add(MaxPooling2D(pool_size=(3,3)))

    model.add(Conv2D(256,(2,2), activation="relu"))
    model.add(MaxPooling2D(pool_size=(2,2)))

    model.add(Flatten())

    model.add(Dense(units=128, activation="relu"))

    model.add(Dense(units=1, activation="sigmoid"))

    model.compile(optimizer='adam', loss='binary_crossentropy', metrics=['accuracy'])
    return model

IA = IA_create()

IA.summary()

# entrenament de la red neuronal convulcional per distingir entre gossos y gats.

d_entrenament = "C:/Users/roger/OneDrive/Documents/TDR/Red neuronal convulcional/dades/entrenament"
d_test = "C:/Users/roger/OneDrive/Documents/TDR/Red neuronal convulcional/dades/test"

train_datagen = ImageDataGenerator(
    rescale=1./255,
    shear_range=0.2,
    zoom_range=0.2,
    horizontal_flip=True
)

test_datagen = ImageDataGenerator(rescale=1./255)

train_generator = train_datagen.flow_from_directory(
    d_entrenament,
    target_size=(64, 64),
    batch_size=32,
    class_mode='binary'
)

test_generator = test_datagen.flow_from_directory(
    d_test,
    target_size=(64, 64),
    batch_size=32,
    class_mode='binary'
)

IA.fit(train_generator, epochs=30, validation_data=test_generator)

IA.save("C:/Users/roger/OneDrive/Documents/TDR/Red neuronal convulcional/model_IA")
























jo soc el millor :D